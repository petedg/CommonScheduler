<UserControl x:Class="CommonScheduler.MenuComponents.Controls.TopMenuButtonControl"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             mc:Ignorable="d" 
             d:DesignHeight="100" d:DesignWidth="140" Name="topMenuButtonControl">
    <Button Style="{StaticResource topMenuButton}" Click="Button_Click">
        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="40" />
                <RowDefinition Height="40" />
            </Grid.RowDefinitions>
            <Image Source="{Binding ElementName=topMenuButtonControl, Path=TopMenuButtonImageSource}" Grid.Row="0" />
            <TextBlock Text="{Binding ElementName=topMenuButtonControl, Path=TopMenuButtonText}" Grid.Row="1" TextWrapping="Wrap" />
        </Grid>
    </Button>
</UserControl>
